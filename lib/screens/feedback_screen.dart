import 'package:flutter/material.dart';

class FeedbackScreen extends StatelessWidget {
  const FeedbackScreen({super.key, required void Function(AppBar appBar) onUpdateAppBar});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}